name: ririkan
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # Navigation (deep link対応: 通知タップ→アプリ詳細)
  go_router: ^14.8.1

  # APIキー・トークンのオンデバイス保管（Must#4）
  flutter_secure_storage: ^9.2.4

  # 多言語対応（日本語＋英語、初期スコープ）
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter

  uuid: ^4.5.1

  # レポートエクスポート（Should機能）: PDF/CSV生成・端末内保存・共有
  pdf: ^3.11.1
  csv: ^6.0.0
  path_provider: ^2.1.5
  share_plus: ^10.1.4

  # ホーム画面ウィジェット（Should機能）: Dart⇔ネイティブ間の共有データ橋渡し
  home_widget: ^0.9.3

  # App Store Connect API 実接続用（GET /v1/apps によるアプリ一覧取得）。
  # JWT(ES256)署名にdart_jsonwebtoken、HTTP呼び出しにhttpを使用する。
  http: ^1.2.2
  dart_jsonwebtoken: ^2.14.0

  # RevenueCat連携（売上・DL数、Should機能）: Charts & Metrics APIはOAuth 2.0
  # (Authorization Code + PKCE)必須のため、システムブラウザ経由の認可フローに
  # flutter_web_auth_2、PKCEのcode_challenge(S256)算出にcryptoを使用する。
  flutter_web_auth_2: ^4.1.0
  crypto: ^3.0.6

  # 毎朝の状態サマリー通知（Should機能）: ローカル通知のスケジューリングに
  # flutter_local_notifications、毎日同一時刻(端末のローカルタイムゾーン)への
  # 繰り返しスケジューリングにtimezone/flutter_timezoneを使用する。
  flutter_local_notifications: ^19.4.2
  timezone: ^0.10.1
  flutter_timezone: ^4.1.1

  # マネタイズ（広告表示＋月$1で広告非表示）: バナー広告表示にgoogle_mobile_ads、
  # 「広告を消す」購入(App内課金)にRevenueCatのクライアントSDKであるpurchases_flutter
  # を使用する(REST APIベースの分析用RevenueCat連携とは別物、SDKキーも別)。
  google_mobile_ads: ^9.1.0
  purchases_flutter: ^9.5.4

  # ローカル永続化(LocalStoreService)のクラウドバックアップ: Firebase Auth
  # 匿名認証で発行されたUIDをドキュメントIDにFirestoreへ登録アプリ一覧・
  # プランをバックアップする(lib/services/cloud_sync_service.dart参照)。
  # ログイン画面は無く、複数端末間の同期も保証しない(同一インストール内での
  # 復旧用途に限定)。
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # path_providerが内部で使うプラットフォームインターフェース。テストコードから
  # PathProviderPlatform.instanceを直接フェイクに差し替えるため明示的な依存にする
  # (これまでは推移的依存として解決されていたのみだった)。
  path_provider_platform_interface: ^2.1.2

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # アプリアイコン（assets/icon/）からiOS/Androidの各解像度アイコンを生成する。
  # `dart run flutter_launcher_icons` で再生成できる（下の flutter_launcher_icons: 設定参照）。
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # ARBファイル（lib/l10n/）からAppLocalizationsを自動生成する（多言語対応、l10n.yaml参照）。
  generate: true

  # PDFエクスポートの日本語表示用フォント（Noto Sans JP, SIL Open Font License 1.1）
  # 出典: https://github.com/google/fonts/tree/main/ofl/notosansjp
  assets:
    - assets/fonts/NotoSansJP-Variable.ttf

# アプリアイコン生成設定（flutter_launcher_icons）。
# 変更したら `dart run flutter_launcher_icons` を再実行してiOS/Androidの
# 各解像度アイコンを再生成すること。
flutter_launcher_icons:
  image_path: "assets/icon/icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  adaptive_icon_background: "#0A2C49"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

